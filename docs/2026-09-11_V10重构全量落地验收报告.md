# V10 重构 · 全量落地验收报告（2026-09-11）

> 范围：`docs/2026-09-11_V10架构纠偏与全链路贯通重构方案.md` Phase A–G 全部 7 个阶段。
> 结论：**7 个 Phase 全部完成，验证全绿**。3 项存量问题与本轮无关，已单列。

## 总览

| Phase | 内容 | 状态 | 验证 |
|---|---|---|---|
| A | core 分层纠偏 + AppContext 显式注入 + 23 路由迁移 | ✅ | verify_appctx PASS；门禁基线收紧；core/ 反向 import 零命中 |
| B | 选股 SQL pivot（批量化） | ✅ | test_bars_batch_pivot 4 测试绿 + screen 回归绿 |
| C | 前端 5 组件能力门控 can("trading") | ✅ | 6 组件命中门控；vite build 绿 |
| D | 前端 TS 化 + features/ 域拆分 | ✅* | tsc --noEmit 零错误；WS_EVENTS 14 处引用；features/ 落地；build 绿 |
| E | Electron 安全加固 | ✅ | CSP/导航白名单/开窗拦截/协议校验/sandbox |
| F | 补齐测试覆盖 | ✅ | 17 个新测试全绿；smoke2 断言 30+（≥10 达标） |
| G | shim 清理 | ✅ | 12 文件删除；引用零命中；compileall + 启动验证 PASS |

\* Phase D 的 DoD 四项全部达成；D2（openapi-typescript 生成）/D3（api.js 全量 typed client）/D5（123 个组件逐个 .tsx 化）/D8（styles.css design token 化）体量大，本轮落地基础设施 + 试点，后续可渐进推进。

## 各 Phase 明细

### Phase A — AppContext 显式注入（收尾）
- `scripts/verify_appctx.py` 修复嵌套 try/except 后 **RESULT: PASS**：health 200 / config 200 / ready 503（无券商就绪语义正确）、`ctx.db/broker_manager/job_runtime/data_router/execution` 全非 None、`health_check params: ['ctx']`。
- `scripts/check_appcontext.py --update` 收紧基线（新直接 state import = 0）。
- `app/runtime/eod.py` 补迁移到 `active_context()`；全后端 `compileall` OK。

### Phase B — 选股 SQL pivot
- `datasource/local_store.py` 新增 `get_bars_batch(codes, ...)`：IN 分块（≤900 占位/块）单查询，替代逐只 N 次 SQL。
- `app/data/bars_provider.py`：`_local_batch` 改调批量接口；`_fetch_online` 改 `asyncio.gather` 并发拉取。
- `tests/test_bars_batch_pivot.py`：批量正确性 / 与逐只结果一致 / 200 只×60 日 / 空 code 边界，4 测试绿。

### Phase C — 能力门控
- AccountsGrid（批量下单/撤单/重连 3 个提交按钮）、Algo（提交/暂停/恢复/取消）、QuickTrade（买卖提交）、OrderTicketModal（预检/提交）、Rebalance（生成并下单）全部接入 `usePlatform().can("trading")`，无券商连接时按钮 disabled + 提示。

### Phase E — Electron 安全加固（`frontend/electron/main.cjs`）
- **CSP**：`applySecurityPolicy()` 经 `session.webRequest.onHeadersReceived` 注入本机源响应（script-src 'self'；connect-src 本机 REST+WS；object/frame none；另加 X-Content-Type-Options）。
- **导航防护**：`will-navigate` 仅放行 `http://127.0.0.1:*`，外链转系统浏览器；`setWindowOpenHandler` 拒绝建窗。
- **IPC 收口**：`open-external` 仅放行 http/https（file://、ms-msdt: 等拒绝）。
- **webPreferences**：新增 `sandbox: true`（preload 仅用 ipcRenderer/contextBridge，可安全沙箱化）。
- `node --check` 主进程/preload 语法 OK。

### Phase F — 测试覆盖（17 个新测试）
- `test_webhook_out.py`（6 绿）：事件匹配（精确/`*`/`order.*`）、HMAC 签名格式与可复算、未签名订阅无签名头、首次成功、重试+指数退避序列断言（monkeypatch asyncio.sleep 捕获 [0.5, 1.0]）、重试耗尽 failed 落库+fail_count。
- `test_reconcile.py`（7 绿）：空 WAL、终态核销+WAL 写回 reconciled、数量差异 mismatch、挂单不核销、查无此单轮数保护、跨日 stale、wal_ts 回退。
- `test_appcontext_injection.py`（4 绿）：ctx 槽位非 None、active_context 单例、路由 handler Depends(get_ctx) 签名、端到端 health/config/ready。
- smoke2.py 断言 30+ ≥ 10（F4 达标，无需改动）。

### Phase G — shim 清理（12 文件删除）
- 删除：`app/{state,config,crypto,db,db_migrations}.py`、`gateway/masking.py`、`paper/`（整目录）、`tools/{algo,condition_order,limitup,strategy_runtime}.py`。
- 引用迁移：run.py / check_capability_drift.py / introspect.py / test_capabilities.py → core.config；test_{unit,stage0b,strategy_market,paper,contract_api} → core.state；`tools/rebalance.py` 相对导入 `.limitup` → `engines.limitup`（v1 扫描漏网，启动验证暴露后修复）。
- 配套更新：`build_exe.py` hiddenimports 去 shim 项；`test_crypto.py` 直测 core.crypto（新增字段级加解密回环测试）；`test_unit::test_idempotency` 改测现行 `gateway.idempotency.single_flight`（旧 tools.trading._IDEMPOTENCY API 已不存在）。
- 修复测试夹具与 V10 语义的适配：`_patch_state`/paper 夹具改为 `set_active_context(AppContext(...))`（无 lifespan 时 `active_context()` 每次返回新实例，patch 单实例无效——本轮踩坑沉淀）。
- 回归：test_unit 97 绿、test_paper 16 绿、test_contract_api 16 绿、test_stage0b 13 绿、test_strategy_market 7 绿、test_capabilities 6 绿、test_webhook_out 6 绿、test_reconcile 7 绿、test_crypto 10 绿。

### Phase D — 前端 TS 化 + features/ 拆分
- **D6/D7 事件统一**：新建 `src/shared/events.ts`（WS_EVENTS 单一真源 + 补契约原生 `DEAL` 事件 + 原 lib/events.js 的 EVENT_TYPES/normalizePlatformEvent 并入）；删除 `shared/events.js` 与 `lib/events.js` 双份定义；6 个 `useServerEvents` 裸字符串调用点 + `useSystemWS` + `quoteHub` 改常量引用，**14 处引用 / 7 文件**（DoD ② ≥10 达标）。
- **D4 features/ 拆分**：13 个业务组件迁入 `features/{trading,research,accounts,market,system}/`，全仓静态+动态导入重写（脚本 `frontend/scripts/migrate_features.py`，幂等可重跑）；旧路径引用全仓零命中。
- **D1 tsconfig**：strict + allowJs 渐进策略（include 仅 ts/tsx，存量 JS 由 vite/esbuild 处理）；`tsc --noEmit` 零错误；package.json 新增 `typecheck` 脚本。
- **D5 试点**：shared/events.ts 全类型化（const 断言 + PlatformEvent 接口）。
- **验证**：vite build 绿（695 模块）；events 单测 2 绿。

## 存量问题（本轮未触碰，建议后续跟进）
1. **vitest jsdom 环境全挂**：所有 `@vitest-environment jsdom` 测试报 "Vitest failed to find the current suite"（node 环境测试全过）。涉及文件本轮多数未改动，属 vitest 2.1.9 + 当前运行环境的存量问题。
2. **dataHub.test.js**：`api.dataHubPolicies.mockResolvedValue is not a function`（mock 解析问题，文件未被本轮修改）。
3. **durable scheduler 启动失败**：`phase_misc.py` `await runner.start()` 返回 None（V9 既有，不影响其他能力）。

## 建议下一步
- Phase D 渐进推进：安装 `@types/react` + `openapi-typescript`（需 npm 网络）后做 D2 typed client 与逐域 .tsx 化。
- 按用户节奏统一 git 提交（本轮未做任何提交）。
