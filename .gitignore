# ---------- Python ----------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ---------- 后端构建产物 ----------
backend/build/
backend/dist/
backend/logs/
# 多运行时桥接的嵌入式 Python（由 tools/fetch_runtimes.py 下载，不入库）
backend/runtimes/
# 策略生成器输出目录（tools/strategy_gen.py 生成，不入库）
backend/qmt_strategies/
# ---------- 运行期数据 ----------
# SQLite + 加密主密钥不入库
backend/data/
# 前端构建产物（npm run build 生成）
backend/static/
# exe 同目录自动生成的运行期配置
smart_qmt_config.json
qmt_work_config.json
# 运行期生成的端口发现文件（非源码）
backend/.qmt_work.port
# 运行期测试端口文件（探针排错产物，不入库）
.test_port.txt
# 运行期端口发现文件（多 db 目录实例各自生成，不入库）
backend/.qmt_run_port
backend/.qmt_run_port2
# 行情导出目录（POST /api/v1/market/kline/sync 产物，不入库）
kline_data/
kline_sync/
# 测试输出目录（pytest 落盘日志等）
output/

# ---------- 前端 ----------
# （旧 frontend/ 已于 2026-09-15 V11-R3 退役删除，见 tag archive-frontend-v1）
# frontend-next（新一代前端工程）
# ★ frontend-next/build/ 含 icon.ico / icon.png 打包图标，属源资源，故意不忽略
frontend-next/node_modules/
frontend-next/dist/
frontend-next/dist-electron/
frontend-next/.vite/
frontend-next/release*/
# Vite / Vitest 构建期生成的临时配置副本
*.timestamp-*.mjs

# ---------- 运行期 ----------
*.log
server_*.log
electron_launch.log

# ---------- 编辑器/系统 ----------
.vscode/
.idea/
.DS_Store
Thumbs.db

# ---------- 本地 AI 协作记忆（含内部开发历史，不随公开仓库分发）----------
.workbuddy/

# ---------- 内部开发文档（不随公开仓库分发） ----------
ROADMAP_VERSION_COMPAT.md
docs/QMT量化Agent平台方案.md
docs/基于deepseek-harness的客户端重构方案.md
docs/竞品对比与优化改进方案.md
docs/二轮复查与优化改进计划（2026-08-27）.md
docs/功能层问题与改进优化方案（2026-08-26审计版）.md

# ---------- D1 工程卫生（2026-08-29 全链路计划） ----------
.test_port.txt
r4_git_status.txt

# ---------- 一次性排查/探针落盘产物（不入库） ----------
_file_list*.txt
_*_out.txt
backend/tests/_verify_out.txt
backend/_smoke_srv.log
.workbuddy-ai/

# ---------- 运行期数据 / 缓存 ----------
backend/wal.jsonl
frontend-next/.vite-node/

# 已废弃组件（.dead 后缀标记，勿入库）
frontend-next/src/**/*.dead
_tmp_test/
_scratch_decode.py
/tests/

# 本地测试依赖隔离目录（embedded python pip --target，不入库）
.venv-test/
